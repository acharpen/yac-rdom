# YAC-rdom

YAC-rdom is a minimalistic JavaScript library for creating user interfaces.

### Key Features

- Fine-grained updates. When your state updates only the DOM nodes that depend on it update.
- Real DOM nodes. No Virtual DOM. No dirty checking digest loop.
- Explicit. No hidden rules.
- Small.
